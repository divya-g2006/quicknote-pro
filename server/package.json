{
  "name": "quick-notes-server",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "dev": "nodemon server.js",
    "build": "npm --prefix ../client install && npm --prefix ../client run build",
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^7.5.2",
    "quick-notes-dashboard": "file:.."
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
